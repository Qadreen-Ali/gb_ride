org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# Mapbox secret download token — get from https://account.mapbox.com/access-tokens/
# Create a token with scope "DOWNLOADS:READ" and paste it here
